interface:
  display_name: "Claude Review"
  short_description: "Review a branch with persistent Claude context"
  default_prompt: "Use $claude-review to review my current branch."

policy:
  allow_implicit_invocation: false
